int suma(int a){
    return a + 1;  // Retorna el valor de a incrementado en 1
}

int main() {
    int a = 8;    // Inicializamos a con 8
    int b = 8;     // Inicializamos b con 8
    int r = suma(a);  // Ahora a está inicializada cuando la usamos

    return 0;
}
